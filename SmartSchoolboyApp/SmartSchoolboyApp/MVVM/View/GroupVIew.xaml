<UserControl x:Class="SmartSchoolboyApp.MVVM.View.GroupVIew"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartSchoolboyApp.MVVM.View"
             mc:Ignorable="d" 
             FontFamily="Montserrat"
             FontWeight="Medium"
             xmlns:custom="clr-namespace:SmartSchoolboyApp.Thems.UserControls"
             xmlns:converter="clr-namespace:SmartSchoolboyApp.Converters"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:viewModel="clr-namespace:SmartSchoolboyApp.MVVM.ViewModel"
             d:DesignHeight="450" d:DesignWidth="800" x:Name="MainGroupView">
    
    <UserControl.DataContext>
        <viewModel:GroupViewModel/>
    </UserControl.DataContext>
    
    <UserControl.InputBindings>
        <KeyBinding Command="{Binding Path=AddEditGroupCommand}" Key="N" Modifiers="Ctrl"/>
        <KeyBinding Command="{Binding Path=DeleteGroupCommand}" Key="Delete" Modifiers="Ctrl"/>
    </UserControl.InputBindings>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibility"/>
        <converter:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibility"/>
    </UserControl.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            
        </Grid>

        <Border Grid.Row="1" Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibility}}" Style="{StaticResource listViewBorder}">
            <ListView ItemsSource="{Binding Path=Group}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Width="200" Style="{StaticResource listViewBorderItem}">
                            <StackPanel Orientation="Vertical">

                                <TextBlock Text="{Binding Path=name}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Path=course.name}" TextWrapping="Wrap" Margin="0 5 0 0"/>

                                <Grid Margin="0 5 0 0">

                                    <Button Command="{Binding ElementName=MainGroupView, Path=DataContext.ViewGroup}" CommandParameter="{Binding}" Tag="{StaticResource color9}" HorizontalAlignment="Left" Style="{StaticResource listViewButton}">
                                        <fa:IconImage Icon="Eye" Style="{StaticResource listViewButtonIcon}"/>
                                    </Button>

                                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                        <Button Tag="{StaticResource color6}" ToolTip="Редактировать (Ctrl+N)" Style="{StaticResource listViewButton}">
                                            <fa:IconImage Icon="Edit" Style="{StaticResource listViewButtonIcon}"/>
                                        </Button>

                                        <Button Tag="{StaticResource color4}" ToolTip="Удалить (Ctrl+Del" Style="{StaticResource listViewButton}">
                                            <fa:IconImage Icon="TrashCan" Style="{StaticResource listViewButtonIcon}"/>
                                        </Button>
                                    </StackPanel>

                                </Grid>

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <custom:LoadingSpinner Grid.Row="1">
            <custom:LoadingSpinner.Visibility>
                <Binding Path="IsLoading" Converter="{StaticResource BooleanToVisibility}"/>
            </custom:LoadingSpinner.Visibility>
        </custom:LoadingSpinner>

    </Grid>
    
</UserControl>
