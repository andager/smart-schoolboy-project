<UserControl x:Class="SmartSchoolboyApp.MVVM.View.HomeGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartSchoolboyApp.MVVM.View"
             mc:Ignorable="d" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             d:DesignHeight="450" d:DesignWidth="800" Background="{StaticResource backgroundGradient}">
    
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <RadioButton Tag="{StaticResource color1}" Style="{StaticResource menuGroupButton}">
                <StackPanel Orientation="Horizontal">
                    <fa:IconImage Icon="GraduationCap" Style="{StaticResource menuGroupButtonIcon}"/>
                    <TextBlock Text="Курсы" Style="{StaticResource menuButtonText}"/>
                </StackPanel>
            </RadioButton>
        </StackPanel>

        <ContentControl Content="{Binding Path=CurrentChildView}" Grid.Row="1"/>
        
    </Grid>
</UserControl>
